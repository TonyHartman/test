import pandas as pd
'''
количество студентов с одинаковыми баллами по всем трём предметам
'''
f = pd.read_csv('use.csv')
print(f.groupby(["Математика", "Физика", "Русский язык"]).size()[f.groupby(["Математика", "Физика", "Русский язык"]).size() != 1].sum())